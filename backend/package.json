{
  "name": "nirmaan-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build-frontend": "cd ../frontend && npm install && npm run build && cp -r build ../backend/",
    "install-deps": "cd python && pip install -r requirements.txt"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}